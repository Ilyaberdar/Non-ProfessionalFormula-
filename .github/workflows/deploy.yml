name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Deploy containers
        env:
          REPO_DIR: /home/quant-intelligence/Desktop/github/Non-ProfessionalFormula-
        run: |
          cd "$REPO_DIR"
          ./deploy/deploy.sh
